'use strict';

const express = require('express');
const router = express.Router();
// const getStocks = require('../controllers/getStocksCtrl');

// router.post('/quotes', getStocks.create);

module.exports = router;
